<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.DeezerProyecto" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Colores principales -->
        <item name="colorPrimary">@color/colorVerdeSpotify</item>
        <item name="colorPrimaryVariant">@color/colorVerdeOscuro</item>
        <item name="colorOnPrimary">@android:color/black</item>
        <item name="colorSecondary">@color/colorGris</item>
        <item name="android:colorBackground">@color/colorFondo</item>
        <item name="android:textColor">@android:color/white</item>

        <!-- Compatibilidad con componentes Material -->
        <item name="android:statusBarColor">@color/colorFondo</item>
        <item name="android:navigationBarColor">@color/colorFondo</item>
        <item name="android:windowLightStatusBar">false</item>

        <!-- Botones e inputs -->
        <item name="editTextColor">@android:color/white</item>
        <item name="android:textColorHint">#FFFFFF</item>
    </style>
</resources>
